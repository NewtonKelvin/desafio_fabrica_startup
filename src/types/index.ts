export type LoginType = {
  login: string;
  password: string;
};

export type ProductType = {
  name: string;
  qty: number;
  price: number;
  categories: string[];
};

export type DatabaseId = {
  id: string;
};
